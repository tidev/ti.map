@Library('pipeline-library@ti-cli') _

buildModule {
	// defaults:
	// nodeVersion = '8.9.1' // Must have version set up on Jenkins master before it can be changed
	// sdkVersion = 'latest' // use a master build with ARM64 support
	// androidAPILevel = '23' // if changed, must install on build nodes
}
